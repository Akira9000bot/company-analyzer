# Assets

This directory contains supporting files for the OpenClaw workspace.

## Structure

- `outputs/`: Temporary or generated files from tool executions (e.g., reports, processed data, or media).
- `inputs/`: (Optional) Files provided for processing.

Files in `outputs/` are generally considered ephemeral and may be flagged for cleanup during routine workspace maintenance.